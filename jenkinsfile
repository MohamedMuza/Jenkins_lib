@Library('shared-library') _

pipeline{
    agent any
    stages{
        stage('Demo'){
            steps{
                script
                {       dir('BUILD'){         
                    sh """git clone https://github.com/davidmoten/maven-demo.git
                    cd maven-demo """                
                    mavenbuild()
                    }
                }
            }
        }
    }
}
